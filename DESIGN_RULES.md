
## Button Color Rules

- **Yellow/gold buttons (`bg-[#f5a800]` or similar):** Text MUST be `text-white`, NEVER `text-[#0d1f5c]` or any dark blue. Icons inside yellow buttons must also be white.
- Example: `<a className="bg-[#f5a800] text-white font-bold ...">`
